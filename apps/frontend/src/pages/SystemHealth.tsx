import { memo } from "react";

export default memo(function SystemHealth() {
  return (
    <div className="p-6 text-slate-200">
      <h1 className="text-xl font-semibold">System Health</h1>
      <p>Placeholder — replace with the real diagnostics UI.</p>
    </div>
  );
});