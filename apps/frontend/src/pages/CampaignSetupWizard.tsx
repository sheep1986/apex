import { memo } from "react";

export default memo(function CampaignSetupWizard() {
  return (
    <div className="p-6 text-slate-200">
      <h1 className="text-xl font-semibold">Campaign Setup Wizard</h1>
      <p>Placeholder — replace with the real wizard when ready.</p>
    </div>
  );
});