{
  "version": 2,
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "echo 'No build needed for server-simple.js'",
  "builds": [
    {
      "src": "./server-simple.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server-simple.js"
    }
  ],
  "crons": [
    {
      "path": "/api/trigger-campaign-executor",
      "schedule": "* * * * *"
    }
  ]
}
